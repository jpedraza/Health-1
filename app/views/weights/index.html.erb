<p id="notice"><%= notice %></p>

<table>
  <tr>
    <th>Weight</th>
	<th>Date</th>
  </tr>

<%= render '/shared/weight' %>
  
<% @weights.each do |weight| %>
  <tr>
    <td><%= weight.weight %></td>
    <td><%= weight.date_of_entry %></td>
  </tr>
<% end %>
</table>

<br/>
