<p id="notice"><%= notice %></p>

<%= render '/shared/weight' %>

<table>
  <tr>
    <th>Weight</th>
	<th>Date</th>
  </tr>

<% @weights.each do |weight| %>
  <tr>
    <td><%= weight.weight %></td>
    <td><%= weight.date_of_entry %></td>
  </tr>
<% end %>
</table>

<br/>
